fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
apscheduler==3.10.4
httpx==0.27.2
python-dotenv==1.0.1

# Note: app/providers/sofascore.py is kept in the repo for reference (it
# was tried first) but is no longer imported by sync.py — SofaScore's
# Cloudflare bot-check reliably blocks server hosts like Render. If you
# revive it, you'll need to add back: curl_cffi>=0.11.4
