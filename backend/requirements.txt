fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
apscheduler==3.10.4
httpx==0.27.2
python-dotenv==1.0.1

# Used by app/providers/sofascore.py — plain httpx/requests get a 403
# Cloudflare challenge from SofaScore's API; curl_cffi's browser TLS
# impersonation is what gets past it (see that file's docstring).
curl_cffi>=0.11.4
